import { createGlobalStyle } from 'styled-components'

const EstiloGlocal = createGlobalStyle`
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Roboto, sans-serif;
  }
`

export default EstiloGlocal
